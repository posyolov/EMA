@model IEnumerable<Repository.Position>

<div class="content">
    <table class="table">
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Title</td>
            </tr>
        }
    </table>
</div>
