<Window x:Class="EMA.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EMA"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="MainMenu.xaml"/>
                <ResourceDictionary Source="PositionsTree.xaml"/>
                <ResourceDictionary Source="EntriesTree.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <DockPanel>
        <ContentControl DataContext="{Binding MainMenuVM}"
                        Template="{StaticResource MainMenu}" 
                        DockPanel.Dock="Top"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <ContentControl Grid.Column="0"
                DataContext="{Binding PositionsTreeVM}"
                Template="{StaticResource PositionsTree}"/>

            <GridSplitter Grid.Column="1" Width="10" VerticalAlignment="Stretch" HorizontalAlignment="Center"/>

            <ContentControl Grid.Column="2"
                DataContext="{Binding EntriesTreeVM}"
                Template="{StaticResource EntriesTree}"/>
        </Grid>

    </DockPanel>

</Window>
